const sqlCreds = {
    connectionLimit : 10,
    host            : 'localhost',
    user            : 'root',
    password        : 'root', //blank for window root for mac
    database        : 'roku_front',
    port            : '8889' //8889 for MAC users
}

module.exports = sqlCreds;